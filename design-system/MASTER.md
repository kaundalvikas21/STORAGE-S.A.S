# DESIGN.md — Storage S.A.S · storagebogota.com
Variant: «El Calculista» · Kinetic Interactive (bento, tool-first, Prompt 4)
Dials: DESIGN_VARIANCE 7 · MOTION_INTENSITY 7 · VISUAL_DENSITY 6

> Generated first with `ui-ux-pro-max` (`design-system/storage-sas/MASTER.md`: Flat Design, blue/orange, Inter + Playfair). That output is **overridden** here per the «El Calculista» brief. This file is the single source of truth; `app/globals.css` mirrors it 1:1. The palette now comes from the client's logo (client checklist, 2026-09); every value stays a CSS variable so a refined brand spec is still one edit. Supersedes «El Sistema» (kept on branch `el-sistema`); «El Contenedor» on `el-contenedor`, «La Bóveda» on `la-boveda`.

## 1. Colors (CSS variables, `:root`)
**Palette source (client checklist, 2026-09, confirmed by the client):** the Storage S.A.S logo on storagebogota.com, exact pixel values from the PNG (no rounding): yellow `#EFE635` field, near-black `#262324` type, kraft `#866A41` cardboard box. Hover and tint shades are derived from those three. Replaces the pending-brand indigo + lime.

| Token | Value | Use |
|---|---|---|
| `--bg` | `#F5F6F8` | page canvas (cool gray between bento cells) |
| `--bg-deep` | `#14161B` | the dark featured-cell fill (one per band) |
| `--surface` | `#FFFFFF` | bento cells, cards |
| `--ink` | `#14161B` | headings (16.7:1 on bg) |
| `--ink-2` | `#333845` | body text (10.8:1 on bg) |
| `--muted` | `#565D6B` | secondary text (6.1:1) |
| `--muted-2` | `#5F6673` | captions (5.3:1 — passes AA everywhere) |
| `--line` | `#E3E6EC` | cell hairlines |
| `--brand` | `#EFE635` | logo yellow (exact): primary CTA FILL only, always with `--on-brand`. Never text on light (1.2:1 on white) |
| `--brand-deep` | `#D9CF22` | primary CTA hover + its 1px edge on white (11.1:1 with `--on-brand`) |
| `--on-brand` | `#14161B` | label on yellow (13.8:1) |
| `--primary` | `#262324` | logo near-black (exact): links, icons, active borders, iso-unit stroke (15.6:1 on white) |
| `--primary-deep` | `#6B5434` | dark kraft (derived from `#866A41`): link/title hover (7.1:1 on white) |
| `--primary-soft` | `#FCFADB` | yellow tint (18% `#EFE635`): selected cells, size wells, contact band, NAP band, iso prism |
| `--on-primary` | `#FFFFFF` | white on near-black |
| `--accent` | `#866A41` | logo kraft (exact): tags and availability chips as FILLS with `--on-accent`. Never body text |
| `--accent-deep` | `#6B5434` | kraft chip border/hover |
| `--accent-soft` | `#F2ECE3` | kraft tint (hot size tier well, mega-menu priority column) |
| `--on-accent` | `#FFFFFF` | white on kraft (5.1:1; near-black would only reach 3.6:1) |
| `--ring` | `#262324` | focus-visible ring (with offset) |
| `--whatsapp` | `#25D366` | WhatsApp brand green: the floating bubble ONLY (§8.18), white icon on it |
| `--whatsapp-deep` | `#1EBE5B` | bubble hover |

**Dark cells** (`.dark-cell` in globals.css): a local CSS-variable scope, not a dark mode. Inside it the same tokens resolve to: bg `#14161B`, surface `#1B1E26`, ink `#F5F6F8` (16.7:1), ink-2 `#C3C9D6`, muted `#9BA3B5` (7.2:1), line `rgba(255,255,255,.12)`, primary AND accent `#EFE635` (13.8:1: yellow on near-black is the logo's own pairing; kraft would drop to 3.6:1), primary-deep `#F6F08F`, on-primary/on-accent `#14161B`, ring `#EFE635`. Components stay zero-hex.

## 2. Typography
- Display: **Bricolage Grotesque** (variable, opsz 12-96) — `--font-display`. Headings 600-700, tracking -0.02em. Kinetic type ONLY in the hero (word-level rise on load); static everywhere else.
- Body: **Inter** — `--font-body`. `--font-mono` aliases body (safety net, unused).
- Scale: `--t-display clamp(2.75rem, 1.75rem + 3.5vw, 4.25rem)` (hero + closing; capped so the H1 holds 2 lines in the hero's left column) · `--t-3xl clamp(1.75rem,…,2.5rem)` · `--t-2xl clamp(1.25rem,…,1.5rem)`.
- Every m³ figure, range and stat in tabular numerals (`.tnum`).
- `.eyebrow`: 12px/500, tracking 0.08em, uppercase, `--muted`. Max 5 of 15 sections, 4 spent: Hero (the stats pill above the H1, `rounded-lg` while it wraps, `rounded-full` from `md`), SedeGrid, Reviews, FAQ. Cómo funciona, ContactBand and SocialFeed carry none: their headings follow the standard section hierarchy (display h2).

## 3. Spacing
8-pt system. Section padding `py-14` mobile → `py-20` md → `py-24` lg. Container max 1280px, gutters 20/32/40px. Bento bands: `gap-4 md:gap-5` between cells, cell padding 24-40px.

## 4. Radius
`--r-sm 8px` (chips, pills, slider) · `--r-md 12px` (buttons, inputs, inner elements) · `--r-lg 16px` (bento cells) · `--r-xl 24px` (closing frosted panel, mega-menu, pop-up dialog). One system: cells 16, buttons 12, chips 8.

## 5. Shadow (dimensional, not glassy)
- `--sh-1: 0 1px 2px rgba(20,22,27,0.06)` resting cells · `--sh-2: 0 4px 16px rgba(20,22,27,0.07)` cell hover lift (pairs with `-translate-y-0.5`) · `--sh-3: 0 12px 32px rgba(20,22,27,0.10)` mega-menu, dark cells, pop-up dialog · `--sh-brass: var(--sh-1)` (legacy alias, unused).
- `:active` on buttons: `scale-[0.98]`. Layered subtle noise: `body::after` fixed grain overlay at 4% (feTurbulence data URI, `pointer-events-none`).

## 6. Motion (springy but disciplined)
- Easing `--ease: cubic-bezier(0.22, 1, 0.36, 1)` (spring-out). Durations `--d-fast 150ms` · `--d 300ms` · `--d-slow 450ms`.
- Reveals: opacity + 12px rise, 300ms spring (`bounce 0.2`), stagger 60ms, once (`whileInView`, -10% margin).
- Exactly three hero-level interactive moments: (1) hero word-stagger over the backdrop plate; (2) intent cards select an m³ profile and pre-fill the calculator deep link; (3) sede map pins highlight from card hover/focus (pure CSS `:has()`), Calle 197 pin pulses once.
- Restrained elsewhere: cell hover lift, marquee (single, pausable), snap-scroll reviews, magnetic Cotizar (≤8px, motion values only), count-ups once, sede cards re-flow with `layout` when sorted by proximity, the Cómo funciona hairline draws in once.
- `prefers-reduced-motion: reduce` → everything instant: H1 static, showcase frozen, marquee becomes a static wrapped row, count-ups show final value, magnetic inert, sede re-sort jumps without layout animation.
- **Interaction tokens**: `--hover-lift: 2px` (card/button hover rise; the existing `-translate-y-0.5` utility equals it, so current card classes are conformant) · `--hover-scale: 1.02` (reserved for scale-based hover where lift doesn't fit) · `--photo-zoom: 1.05` (image scale inside an `overflow-hidden` frame on card hover, `--d-slow` timing; the frame never grows). Aliases NOT added: the polish brief's `--ease-premium` / `--transition-fast` / `--transition-base` are already `--ease` / `--d-fast` / `--d`; never duplicate them. Text links use an underline-draw (`.link-draw`, background-size 0→100% left→right, `--d-fast`); it never applies to buttons or cards.

## 7. Component rules
- **Buttons**: `--r-md`, ≥48px tall, sentence case, max 3 words, `font-medium`. Primary = logo-yellow `--brand` fill + `--on-brand` label + `--brand-deep` 1px edge (hover fills `--brand-deep`); Secondary = near-black `--ink` outline on `--surface`, inverting on hover; Ghost = borderless neutral. `cursor-pointer`, visible focus ring. **Icons:** every CTA button carries one LEADING semantic icon for its intent, 18px regular, lifting 2px on hover (`CtaIcon` / `ctaIcons` in `components/Button.tsx`): Calcular mi espacio → Calculator, Cotizar → Receipt, Ver sedes → MapPin, Llamar → Phone, Enviar y continuar por WhatsApp (the /cotizar/ submit) → WhatsappLogo, Suscribirme → EnvelopeSimple. Exception: the two header CTAs drop their icon between 1024 and 1279px, where nav + both buttons share one row and "Calcular mi espacio" would otherwise wrap. Buttons never carry a trailing arrow; text links ("Ver bodegas →", "Ver las 7 sedes →") keep the arrow as the link affordance. Header logo: the client's PNG (`public/site-logo-storage-sas.png`, 189×94, same file as the live site) at 56px tall via `next/image`, `priority` (above the fold; nav row = 80px, the header cap); the same file heads the footer NAP band at 64px (lazy). PENDIENTE: an SVG or 2x PNG for sharper rendering on retina screens.
- **Inner pages** (first: `/cotizar/`, `/calculadora-de-espacio/`): route groups carry the chrome, pages never import it. `app/(site)/layout.tsx` = full Header/Footer/sticky bar/BackToTop/Popups; `app/(conversion)/layout.tsx` = checkout chrome (`ConversionHeader`: logo + phone; footer = `NapBand` only; sticky bar with Llamar only; no pop-ups). Under the header: `Breadcrumb` ("Inicio › Página", 13px `--muted`, `CaretRight` separators, emits BreadcrumbList JSON-LD). One h1 at `text-3xl` display, intro `max-w-[65ch]`. Every `<main id="main" tabIndex={-1}>` (skip link + BackToTop focus target). Metadata via `pageMeta()` and JSON-LD via `SchemaScript` + `lib/schema.ts` builders.
- **Sedes hub** (`/sedes/`, spec T4): h1 + intro, then the homepage `SedeMap` cell full width (lg only); `SedeFeatured` split card for `sedePages[0]` (kraft badge, facts line, Sabana Norte catchment, secondary "Ver sede" + "Cómo llegar" Google Maps text link); `SedeGrid` bento for the other three pages (first spans two rows with all its addresses, the other two horizontal; zone chips from `coverage`; never three equal cards); `CoverageProse` on `--primary-soft`, prose left + `coveragePhoto` right at lg (stacks below the text on mobile), one up-link to the Bodegaje pillar; `ClosingBand` with page title/body and the site's own `Cotizar` + `Calcular mi espacio` (no "Agendar visita" wording, decided 2026-09-15). `<main>` carries `.sede-band` so the Leaflet styles apply and card hover lights its pin. ItemList JSON-LD of the four sede URLs.
- **Quote form** (`components/conversion/QuoteForm`): four fields on one screen. Labels above, 16px inputs (Popups classes), `noValidate` with inline `role="alert"` errors after the first submit, focus to the first invalid field. Size = radio cards, cuándo = radio chips (`--r-sm`, 44px): visually hidden native radios, selected = `border-primary bg-primary-soft shadow-2` (IntentCards idiom), focus ring via `has-[:focus-visible]`. Full-width primary submit. Trust rail = the page's one dark cell (static stats, no count-up beside a form) + `ShieldCheck` security line.
- **Calculator** (`components/conversion/Calculator`): rooms as white cells in CSS columns (7 uneven rooms, no empty cell), rows with name + always-visible "≈ X m³ cada uno", 44px `Minus`/`Plus` steppers (`aria-disabled` at 0 so focus never drops). Total bar = dark cell, `sticky` at the tool's bottom (above the mobile sticky bar below md), yellow band label, "Ver mi cotización" → `/cotizar/?tamano=`.
- **Accordion** (`.faq-item`, `FaqList`): native `<details name>`, exclusive by HTML, no JS. Opening one panel closes its sibling; browsers without `name` support degrade to multi-open. Height animates via `::details-content` behind `@supports (interpolate-size: allow-keywords)`.
- **Cells/cards**: `--r-lg`, `--surface` fill, `1px --line` border, `--sh-1`; hover `--sh-2` + `-translate-y-0.5`. One dark featured cell max per band. Background diversity per band: white + dark + yellow-soft/photo.
- **Sede cards** (`SedeGrid` + `SedeList`, approved artifact v2, checklist §2): one slim card per physical point (7), each matching a map pin by `id` with the same name and address: 44px thumbnail, name + zone (+ kraft badge), street address, sizes hint from xl. `--r-md` rows with `gap-2` so the seven fit beside the map (section ~1.140px at 1440, was 1.694). One column beside the map at lg; 2-up below lg with the first card spanning both columns so seven cards leave no empty cell. Server-rendered; `SedeList` lays out the header (heading left, "Ordenar por cercanía" button inline with "Ver las 7 sedes" on the right, status line below), the map + list band, and only reorders the cards after an opt-in geolocation request, adding a distance chip. A slim quote row (`Cotizar`) closes the section. Names and addresses come from the client's "Estamos en toda Bogotá" banner (nomenclature PENDIENTE CONFIRMAR).
- **Sede map cell** (`SedeMap` + `SedeLeafletMap`): a `.dark-cell` in the spirit of that banner: Esri dark gray tiles (muted further by `.sede-tiles`), logo-yellow pins with a soft glow (featured pin larger, pulses once), a dashed yellow polyline linking the sedes, the banner's headline as a caption top-left ("Estamos en **toda Bogotá**"), zoom buttons bottom-left, popups on `--surface` with name, address and "Cómo llegar". lg+ only.
- **Kraft accent**: tags and availability chips ("Nueva sede", "Alta disponibilidad", "Recomendado" on light). Never body text, never decorative. On dark cells the accent resolves to the logo yellow.
- **Numbers**: `.tnum` always; `--ink`/`--primary` on light, `--accent` (yellow) on dark cells.
- **Pop-ups** (`Popups`): native `<dialog>` (`showModal`: focus trap, Esc, top layer), `--r-xl`, `--sh-3`, scrim `dialog::backdrop` at `--bg-deep` 60%. Close button 44px with `aria-label`. One at a time, each at most once per session; newsletter frequency-capped in localStorage. Form labels above inputs, 16px input text (no iOS zoom), error line below with `role="alert"`.
- **Imagery**: `next/image` with Spanish alt in sede cards and the Bodegaje showcase; the iso-unit illustration is inline SVG consuming tokens. The sede map is a real Leaflet map (desktop-only cell): grayscale-filtered tiles (`.sede-tiles`), token-styled divIcon pins and popups, scroll-zoom off. Photography lives in one manifest (`content/images.ts`: src, Spanish alt, credit; the client's real sede photos swap there). Since 2026-09-14 every slot is a generated local image in `public/img/` (no external host); `sede-alt-3` is the /sedes/ coverage photo; `sede-alt-1..2` and `segment-empresa-alt` are unused alternates. Every photo renders through the `<Photo>` wrapper: `overflow-hidden` token-radius frame, `object-cover`, blur placeholder, a unifying `--primary-soft` multiply tint so mixed sources read as one art-directed set, optional bottom scrim (`--bg-deep` gradient) when text sits above, and `--photo-zoom` on card hover. Text is never baked into images. Exactly one photo carries `priority`: the hero backdrop, which IS the LCP element now that the hero has one; every other slot stays lazy so nothing competes with it. Art direction (client checklist §1): storage first, not moving; Colombian city or hills behind trucks, forklifts and storage units.
- **Icons**: Phosphor 16-24px `weight="regular"` (stars `fill`). One family; no hand-rolled SVG icons.
- **Banned**: glass/backdrop-blur, gradient text, neon glows, three.js, scroll-jacking, autoplay video, second marquee, cursor gimmicks.

## 8. Declared exceptions (do not "fix")
1. **Hand-rolled SVG illustrations** (the `IsoBox` volume ladder in `IntentCards`, plus the noise data-URI): token-filled, `aria-hidden`, and stepped 0.55-1.0 so the four tiers read as a size ladder rather than four identical cards; each open unit holds the contents its hint copy names (cajas / cama + nevera / sofá + mesa / estibas apiladas) so picture and words agree. The Phosphor-only rule covers *icons* and still stands.
1b. **External map tiles** (`SedeLeafletMap`): the only external asset host on the page besides the feed widget (§8.15). Esri World Dark Gray Canvas tiles (keyless, no watermark; dark to match the client's banner and the `.dark-cell` map) replaced tile.openstreetmap.org, which answered "Access blocked" (OSMF tile policy); CARTO was tried first but its keyless tiles now carry an "API KEY REQUIRED" watermark. Esri's terms expect an ArcGIS account for commercial traffic: the client's Esri, MapTiler or Stadia key is PENDIENTE and swaps in `content/site.ts` `mapTiles` only. Attribution control must stay visible. Map chunk + tiles load only at lg+ (mobile never pays for them).
2. **Marquee** (`ClientLogos`): the single permitted marquee. CSS-only, pauses on hover/focus, both edges fade via `mask-image` (12% each side), static wrapped row without the fade under reduced motion.
3. **Dark inverted cells** (`.dark-cell`: SiloDoors featured, TrustBar, FAQ left, ClosingBand): core to this variant's bento rhythm. A local variable scope, not a theme flip — the page theme stays light, locked.
4. **Second accent** (kraft `--accent`, yellow inside dark cells): the logo carries three colours, so the page does too, each with one job: yellow fills the primary CTA, near-black carries links and icons, kraft marks tags and availability. Contrast rules in §1.
5. ~~Hero 5th element~~ **(withdrawn)**: the hero carried an m³ visualizer as a declared fifth element. It was removed at the client's request; the hero is back to the canonical four (eyebrow, headline, subtext, CTAs) and the backdrop plate carries the right-hand side. Number kept so the §8.x references in code stay stable.
6. **Auto-cycling showcase** (Bodegaje featured cell): pausable via button + hover/focus, with one real dot button per image (44px targets, active dot widens in `--brand`) that jumps to it and restarts the timer. The card is not a single `<Link>`: its title link is stretched over the card and the showcase sits above it (`z-10`), so the dots and pause never navigate. Reduced motion: no autoplay or parallax, dots still switch images. Not autoplay video.
7. **Client logos as PNG** inside the marquee: the real client and partner marks from storagebogota.com (`public/client_logos`, ~65-130px tall), each centred in an equal 128×48 slot (fixed height, width by ratio, never stretched), grayscale at 80% opacity at rest and full colour on hover, the client name as a native `title` tooltip. Vector versions are PENDIENTE for sharper 2x rendering.
8. Em/en dash ban upheld in new copy (`·` and `→` only). Placeholder addresses now read `(PENDIENTE CONFIRMAR nomenclatura)` without dashes.
9. **Closing CTA pair**: brief asked for "No estoy seguro · calcular" (5 words). Adapted to an inline button row, primary `Cotizar` (magnetic) + secondary `Calcular mi espacio`, with the microcopy merged into one muted line *after* the buttons ("¿No estás seguro?" + the reassurance sentence). Respects the 3-word CTA cap and the single Calcular wording. The closing band's backdrop photo is the one `<Photo>` with `zoom={false}`: a full-bleed band is not a card, so `--photo-zoom` has no frame to move inside. The client's "more CTAs" request (checklist §6) reuses the same two labels: the SedeGrid quote row and the exit pop-up say `Cotizar`, never a third wording.
10. `AnimatedNumber` uses framer motion values (`useMotionValue` + `animate`), not a rAF loop touching React state.
11. **Frosted panel** (`ClosingBand`): the §7 glass/backdrop-blur ban holds everywhere except the closing band, where the message sits on `bg-surface/40 backdrop-blur-xl` inside a `--line` hairline at `--r-xl`. Justified because the band is the page's only full-bleed photo: the panel separates message from photograph without stacking another flat scrim. The backdrop is now the generated night plate (`/img/cta-closing.png`: lit storage units in front of Bogotá and Monserrate) under the same dark scrim; re-measure panel contrast whenever the plate changes (previous plate measured 12.8:1 / 8.4:1 / 5.5:1 for headline / body / microcopy). Where `backdrop-filter` is unsupported the 40% fill alone still clears AA.
12. **Hero backdrop photo** (`heroBackdrop` -> `/img/hero_bg.png`): a yellow-door storage facility with an open unit, a forklift carrying a pallet and a staff member walking in, Bogotá's hills and Monserrate behind (replaced the earlier truck plate `hero_img_bg_3.png`, which is kept but unused). The plate is built with an empty fog band on the left, exactly where the H1 and CTAs sit, under the existing white veil (`bg-surface/40 lg:bg-surface/15` plus a left-weighted `from-surface` gradient). Heavier below `lg`, where `object-cover` crops the empty band away. It is the LCP element and therefore the one `priority` photo (§7). Re-measure H1 contrast if the plate changes again.
13. ~~Sede card overlay~~ **(withdrawn)**: the sede name, zone and badge no longer ride the photo. With seven points the cards became compact thumbnail + text rows, so nothing overlays an image. Number kept for stable references.
14. **FAQ row hover**: no background fill. `hover:bg-bg` painted the page background inside a `--surface` card, reading as a hole and colliding with the card's own corner radius on the first row. The row now shifts title and icon to `--primary`, matching the SiloDoors and SedeGrid card titles.
15. **Engagement pop-ups and feed** (client checklist §6): the newsletter dialog (10s after load) and the exit-intent dialog (10% offer) are client requirements, not decoration. Guard rails: one at a time, once per session, newsletter capped 7 days after a dismissal, exit intent via the pointer leaving the top edge (desktop) or a fast scroll back up on touch (framer scroll velocity, no window scroll listener). The social feed (`SocialFeed` + `SocialGrid`, approved artifact 2026-09-14) is a bento post grid: newest post as a 2×2 feature, then 3 Instagram + 3 Facebook squares (no repeated creative), each tile (`SocialTile`) linking to the post with the caption on hover, 5 columns at lg (exactly two rows), 3 at md, 2 on phones; profile chips with real follower counts; a `Todo · Instagram · Facebook` filter that fades (never hides) the other network so nothing reflows; hover/focus zooms the photo and raises a caption scrim; every tile is a lazy `next/image`, no iframes. Content is a SNAPSHOT of the client's real posts (`content/social-posts.ts`, images in `public/social/`), dated in the foot line; `npm run social:sync` (`scripts/fetch-social.mjs`) regenerates it from the Meta Graph API once the client provides tokens. Newsletter endpoint and offer terms remain PENDIENTE in `content/engagement.ts`.
18. **WhatsApp through the form, and CTA wording** (spec T7 + R4, decided 2026-09-14; supersedes the floating WhatsApp bubble of checklist §5): no page renders a WhatsApp link. Every chat starts at `/cotizar/`, whose submit "Enviar y continuar por WhatsApp" POSTs the lead, fires the conversion event and then opens WhatsApp prefilled (`lib/lead.ts`, the only file that builds the URL). **One declared exception (client request 2026-09-15, for client review):** the floating **WhatsApp bubble** on every route (`WhatsAppWidget`, mounted once in `app/layout.tsx`) behaves like the current site's widget: greeting card "Estamos disponibles, chatea ahora" slides in after each load until dismissed (remembered per session), and the bubble opens WhatsApp directly with the business number and a prefilled message (`waChatUrl`, new tab). No bot yet. WhatsApp green `--whatsapp` is the one non-palette colour, for channel recognition. Trade-off accepted by the client: bubble chats skip the form, so they arrive without attribution or answers. Everything else routes through the form (ContactBand tile "Cotiza y sigue por WhatsApp", Cotizas step, exit pop-up). On the calculator the sticky total bar reserves right padding for the bubble; on mobile the greeting can overlap the total bar until dismissed. Quote-intent labels: "Cotizar" everywhere, "Ver mi cotización" only as the calculator hand-off (spec wording). The submit label is 5 words, a declared break of the 3-word CTA rule: it is the spec/wireframe's exact text, names the next step honestly, and the button is full width so it never wraps. A 44px round **back-to-top** button (`BackToTop`, stacked above the bubble in the same fixed column at z-30, above the mobile sticky bar below md; `--surface` with `--line` border, yellow on hover) appears after one viewport of scroll, scrolls smoothly to the top (instant under reduced motion) and moves focus to `<main id="main">`.
16. **Proximity sort** (`SedeList`): the one sanctioned exception to "Calle 197 always first". It is opt-in (button, never on load), uses browser Geolocation only, and the server-rendered and crawled order is unchanged.
17. **Cómo funciona numbered badges** (`HowItWorks`): the 01/02/03 badges and the centered step columns come back at the client's request, for visual parity with the approved el-sistema reference. The section heading itself follows the standard hierarchy (left-aligned display h2), not el-sistema's small label. Not a generic "Paso 1/2/3" label: the verb stays the heading, the numbers only mark sequence and are `aria-hidden` (the `<ol>` already carries order). Badges use the logo yellow (`--brand` + `--on-brand`, `--brand-deep` edge) instead of el-sistema's blue.
